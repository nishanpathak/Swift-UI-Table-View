import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image("ronaldo sewey")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Fake World Cup")
        }
 
    }
}
